N = int(input())

for _ in range(N):
    s = input()
    count = 0
    answer = 0
    for i in range(len(s)):
        if s[i] == 'O':
            count += 1
        else:
            count = 0
        answer += count
    print(answer)
